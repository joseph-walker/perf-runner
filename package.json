{
	"name": "perf-monorepo",
	"version": "1.0.0",
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"format": "turbo run format",
		"lint": "turbo run lint",
		"test": "turbo run test"
	},
	"devDependencies": {
		"turbo": "^1.10.12"
	},
	"workspaces": [
		"apps/perf-runner-api",
		"apps/perf-runner-ui"
	]
}

{
	"name": "perf-monorepo",
	"version": "1.0.0",
	"scripts": {
		"build": "npm exec turbo run build",
		"dev": "npm exec turbo run dev",
		"format": "npm exec turbo run format",
		"lint": "npm exec turbo run lint",
		"test": "npm exec turbo run test"
	},
	"devDependencies": {
		"turbo": "^1.10.12"
	},
	"workspaces": [
		"apps/perf-runner-api",
		"apps/perf-runner-ui"
	]
}
